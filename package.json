{
  "name": "tennis-scoring",
  "version": "1.0.0",
  "description": "Tennis scoring system implementation for the coding challenge",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.0.7",
    "typescript": "^5.0.0",
    "vitest": "^3.0.7"
  }
}
